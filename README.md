# 所剩下的工作

1. **管理员端登录逻辑变更**
   - 现管理员端采用“输入六位密码解锁进入主页”，数据库中的 `admin` 表可以全部删除（尚未删除）。
   - 当前六位密码仅前端本地存储，后续是否需要后端支持（如接口校验、加密存储等）待进一步讨论和完善。

2. **用户端注册头像上传说明**
   - 目前注册时仍可上传头像（代码未及时移除），实际应禁止注册时上传头像。
   - 头像绑定请与“发放导师卡”流程一并处理。
      - 管理员端实现了导师卡发放功能记得到用户端验证一下能否正常预约
      - 待所有功能都实现了，这种功能与功能之间的联动还需要很多验证

3. **页面美化与交互优化重点**
   - 用户端：重点美化登录页、注册页、首页，提升视觉与体验。
      - 当前登录、注册、信息修改页面输入密码都还没有以*****形式出现
   - 管理员端：因功能较多，以有效的分类组织、流程方便快捷清晰明了为努力方向。

4. **签到核销功能建议**
   - 签到核销可先按自己的思路推进。
   - 建议：用户头像可点击放大（便于人脸识别）。
   - 预约名单支持批量勾选，最后一起签到。

5. **微信云托管**
   - 如有时间精力，移植到云上。
   - 当前数据库删除课程的逻辑是把课程的status改成canceled而不是直接在数据库删，可优化
