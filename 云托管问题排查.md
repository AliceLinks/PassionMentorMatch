# 云托管连接问题排查与修复

## 问题描述
管理端小程序登录时报错：`{code: 500, message: "服务器错误"}`

## 问题原因
1. **路径错误**：前端请求路径缺少 `/api` 前缀
   - 错误：`/admin/login` 
   - 正确：`/api/admin/login`

2. **参数不完整**：unlock 页面只传了 `password`，但后端需要 `username` 和 `password`

## 已修复的文件

### 1. [frontend-admin/pages/unlock/index.js](frontend-admin/pages/unlock/index.js#L13)
```javascript
// 修复前
request.post('/admin/login', { password: val })

// 修复后
request.post('/api/admin/login', { username: 'admin', password: val })
```

### 2. [frontend-admin/pages/admin/login/index.js](frontend-admin/pages/admin/login/index.js#L9)
```javascript
// 修复前
request.post('/admin/login', { username, password })

// 修复后
request.post('/api/admin/login', { username, password })
```

### 3. [frontend-admin/pages/unlock/change.js](frontend-admin/pages/unlock/change.js#L21)
```javascript
// 修复前
request.post('/admin/change-password', { old_password, new_password })

// 修复后
request.post('/api/admin/change-password', { old_password, new_password })
```

## 验证步骤

### 1. 检查云托管服务状态
在微信云托管控制台：
- 进入 `mentor-backend` 服务
- 查看实例状态：应该显示"运行中"
- 查看实例数量：至少 1 个

### 2. 检查健康状态
访问健康检查端点：
```bash
# 在云托管控制台的日志中应该能看到
GET /actuator/health
```

### 3. 测试登录
在微信开发者工具中：
1. 打开管理端小程序
2. 输入默认密码：`admin123`
3. 点击登录
4. 应该成功跳转到首页

### 4. 查看请求日志
在开发者工具控制台查看：
```javascript
// 成功的请求应该显示
wx.cloud.callContainer success
{
  statusCode: 200,
  data: {
    code: 200,
    message: "success",
    data: {
      token: "...",
      role: "admin",
      username: "admin"
    }
  }
}
```

## 常见错误与解决方案

### 错误 1: 500 服务器错误
**原因**：
- 路径不正确（已修复）
- 数据库连接失败
- 环境变量配置错误

**解决**：
1. 检查云托管环境变量：
   ```
   DB_URL=jdbc:mysql://<内网地址>:23524/mentor?...
   DB_USER=root
   DB_PASSWORD=<你的密码>
   ```

2. 查看云托管日志，确认具体错误

### 错误 2: 401 未授权
**原因**：用户名或密码错误

**解决**：
- 默认管理员账号：
  - 用户名：`admin`
  - 密码：`admin123`
- 如果忘记密码，需要在数据库中重置

### 错误 3: wx.cloud is not defined
**原因**：云开发未初始化

**解决**：检查 [app.js](frontend-admin/app.js#L6) 中的初始化代码：
```javascript
wx.cloud.init({
  env: 'prod-8glbi5hp12efd72e',
  traceUser: true
});
```

### 错误 4: 数据库连接超时
**原因**：
- 使用了公网地址而非内网地址
- 数据库白名单未配置
- 端口号错误

**解决**：
1. 确认使用内网地址：`sh-cynosdbmysql-grp-5a8ugz1y.sql.tencentcdb.com`
2. 确认端口号：`23524`（不是默认的 3306）
3. 在云数据库控制台添加云托管的网络到白名单

## 测试完整流程

### 用户端测试
1. 打开用户端小程序
2. 点击"微信登录"
3. 查看是否能获取课程列表
4. 尝试预约课程

### 管理端测试
1. 打开管理端小程序
2. 输入密码 `admin123` 解锁
3. 进入"本周课程"查看课程列表
4. 点击某个课程查看预约情况
5. 尝试签到功能

## 性能监控

在云托管控制台查看：
- **响应时间**：正常应该在 100-500ms
- **错误率**：应该接近 0%
- **实例 CPU/内存**：正常应该在 50% 以下

## 后续优化建议

1. **添加重试机制**：
   ```javascript
   // 在 request.js 中添加
   const MAX_RETRY = 3;
   ```

2. **添加请求超时配置**：
   ```javascript
   wx.cloud.callContainer({
     // ...
     timeout: 30000 // 30秒超时
   });
   ```

3. **添加错误上报**：
   将错误日志上报到云托管，便于排查问题

4. **优化数据库连接池**：
   在 [application.yaml](backend/src/main/resources/application.yaml) 中调整：
   ```yaml
   hikari:
     maximumPoolSize: 10  # 根据实际并发调整
     connectionTimeout: 30000
   ```

## 总结

现在所有路径问题已修复，管理端应该可以正常登录了！如果还有问题，请按以下顺序排查：
1. 查看云托管实例状态
2. 查看云托管日志
3. 检查环境变量配置
4. 测试数据库连接

如有任何问题，请提供：
- 云托管日志截图
- 开发者工具控制台错误信息
- 网络请求详情
