# 微信云托管调用配置说明

## 已完成的配置

### 1. 前端配置更新
- ✅ 用户端 `frontend/utils/request.js` 已支持云托管
- ✅ 管理端 `frontend-admin/utils/request.js` 已支持云托管
- ✅ 用户端 `frontend/app.js` 已初始化云环境
- ✅ 管理端 `frontend-admin/app.js` 已初始化云环境

### 2. 云托管配置
- **云环境 ID**: `prod-8glbi5hp12efd72e`
- **服务名称**: `mentor-backend`

## 使用方法

### 方式一：保持原有代码不变（推荐）

现有的所有请求代码**无需修改**，例如：

```javascript
const api = require('../../utils/request.js');

// 获取课程列表（代码不变）
api.get('/api/courses/week?week_start=2025-12-28').then(res => {
  console.log(res);
});

// 管理员查询某日课程（代码不变）
api.get('/api/admin/courses/day?date=2025-12-28').then(res => {
  console.log(res);
});

// 登录（代码不变）
api.post('/api/login', { code: 'xxx' }).then(res => {
  console.log(res);
});
```

**所有现有代码无需改动**，request.js 会自动使用云托管调用！

### 方式二：直接使用 wx.cloud.callContainer（可选）

如果你需要直接调用，可以这样写：

```javascript
wx.cloud.callContainer({
  config: {
    env: 'prod-8glbi5hp12efd72e'
  },
  path: '/api/admin/courses/day?date=2025-12-28',
  method: 'GET',
  header: {
    'X-WX-SERVICE': 'mentor-backend',
    'Content-Type': 'application/json',
    'Authorization': 'Bearer ' + wx.getStorageSync('token')
  },
  success: res => {
    console.log('返回结果:', res.data);
  },
  fail: err => {
    console.error('调用失败:', err);
  }
});
```

## 环境切换

### 切换到云托管模式（生产环境）

在 `utils/request.js` 中：
```javascript
const USE_CLOUD_CONTAINER = true; // 使用云托管
```

### 切换到 HTTP 模式（本地调试）

在 `utils/request.js` 中：
```javascript
const USE_CLOUD_CONTAINER = false; // 使用本地 HTTP
```

## 接口示例

### 用户端接口

```javascript
const api = require('../../utils/request.js');

// 1. 用户登录
api.post('/api/login', { code: 'wx_code' });

// 2. 获取课程列表
api.get('/api/courses/week?week_start=2025-12-28');

// 3. 预约课程
api.post('/api/reservations', { course_id: 123 });

// 4. 我的预约
api.get('/api/user/my-reservations');
```

### 管理端接口

```javascript
const api = require('../../utils/request.js');

// 1. 管理员登录
api.post('/api/admin/login', { username: 'admin', password: 'xxx' });

// 2. 查询某日课程
api.get('/api/admin/courses/day?date=2025-12-28');

// 3. 查询课程预约列表
api.get('/api/admin/courses/123/reservations?checkin_status=all');

// 4. 签到
api.post('/api/admin/reservations/456/checkin');
```

## 调试技巧

### 1. 查看云托管日志
- 进入微信云托管控制台
- 选择 `mentor-backend` 服务
- 点击"日志查询"
- 实时查看请求和响应

### 2. 本地调试
如需本地调试后端，修改 `utils/request.js`：

```javascript
const USE_CLOUD_CONTAINER = false; // 切换到 HTTP 模式
let BASE_URL = 'http://127.0.0.1:8080'; // 本地后端地址
```

### 3. 控制台调试
在小程序中打印请求信息：

```javascript
api.get('/api/courses/week?week_start=2025-12-28')
  .then(res => {
    console.log('成功:', res);
  })
  .catch(err => {
    console.error('失败:', err);
  });
```

## 常见问题

### Q1: 提示 "wx.cloud.callContainer is not a function"
**解决**：确保在 `app.js` 中初始化了云环境：
```javascript
wx.cloud.init({ env: 'prod-8glbi5hp12efd72e' });
```

### Q2: 调用失败，提示 "env not found"
**解决**：检查云环境 ID 是否正确，在云托管控制台确认环境 ID。

### Q3: 401 未授权错误
**解决**：检查 token 是否正确传递，确认用户已登录。

### Q4: 如何切换回本地调试？
**解决**：修改 `utils/request.js` 中的 `USE_CLOUD_CONTAINER = false`。

### Q5: 数据库连接失败
**解决**：
1. 检查云托管环境变量配置
2. 确认使用内网地址
3. 查看云托管日志

## 部署清单

在小程序后台配置：

### 1. 服务器域名配置
- 不需要配置！使用云托管自动处理

### 2. 云开发设置
- 微信开发者工具 → 云开发 → 设置
- 确认环境 ID 为 `prod-8glbi5hp12efd72e`

### 3. 体验版/正式版发布
- 确保 `USE_CLOUD_CONTAINER = true`
- 上传代码
- 提交审核

## 性能优化建议

1. **使用云托管的优势**：
   - 自动负载均衡
   - 弹性伸缩
   - 内网高速访问数据库
   - 免配置域名

2. **监控指标**：
   - 响应时间
   - 错误率
   - 并发数
   - 数据库连接数

## 总结

现在你的小程序已经完全配置好云托管调用：
- ✅ 保持原有代码不变
- ✅ 自动使用云托管
- ✅ 支持一键切换本地/云端
- ✅ 完整的错误处理
- ✅ 自动 token 管理
