# 南京大学 Passion 街舞社导师课系统 接口文档

Base URL: /api  
认证方式: Authorization: Bearer <token>  
Token ；数据库持久化随机 token。  
角色: user / operator / admin / superadmin

统一响应格式:
```json
{
  "code": 200,
  "message": "success",
  "data": {}
}
```
错误示例:
```json
{
  "code": 409,
  "error_code": "409",
  "message": "课程已满"
}
```

分页约定:
- 请求参数: page(>=1), page_size(默认20, 最大100)
- 响应附带:
```json
"meta": { "total": 120, "page": 1, "page_size": 20 }
```
- 常见 code 语义：
  - 200 成功
  - 400 请求参数错误
  - 401 未认证/登录失效
  - 403 权限不足
  - 404 资源不存在
  - 409 业务冲突（如重复预约、名额已满）
  - 500 服务器错误

---

## 1. 用户认证与用户信息

### 1.1 用户注册（手机号 + 密码）
- Method: POST
- URL: /api/user/register
- 权限: 匿名
- 请求:
```json
{ "phone": "13800138000", "password": "plaintext" }
```
- 成功响应:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "token": "TOKEN",
    "user": {
      "id": 123,
      "phone": "13800138000",
      "nickname": null,
      "avatar": null,
      "status": "active"
    }
  }
}
```

- 失败响应: 400/401/500
- 备注: 后端应对密码进行存储,首次注册后返回 token，前端保存并在后续请求携带 Authorization: Bearer TOKEN。注册同时可创建用户记录并返回用户信息。

### 1.2 获取当前登录用户信息
- Method: POST
- URL: /api/user/login
- 权限: 匿名
- 请求:
```json
{ "phone": "13800138000", "password": "plaintext" }
```
- 成功响应:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "token": "TOKEN",
    "user": {
      "id": 123,
      "openid": null,
      "nickname": "昵称",
      "real_name": null,
      "phone": "13800138000",
      "avatar": "url"
    }
  }
}
```
- 失败响应:
  - 400: 参数错误
  - 401: 用户名或密码错误
  - 403: 账号被禁用
  - 500: 服务器错误
- 备注:
前端登录请调用 /api/user/login 并保存返回 token 到本地存储（wx.setStorageSync('token')）。


### 1.3 获取当前登录用户信息
- Method: GET
- URL: /api/user/profile
- 权限: user
- 响应:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 123, 
    "openid": "openid", 
    "nickname": "昵称",
    "real_name": null, 
    "phone": null, 
    "avatar": "url", 
    "status": "active"
  }
}
```
- 失败: 401 登录失效

### 1.4 更新用户信息
- Method: PUT
- URL: /api/user/profile
- 权限: user
- 请求(任意可选字段):
```json
{
   "real_name": "张三", 
   "phone": "13800000000",
   "avatar": "url" 
 }
```
- 响应: 返回更新后的用户
- 校验: 手机号格式、长度限制

---

## 2. 导师卡 (Cards)

### 2.1 获取当前用户卡片列表
- Method: GET
- URL: /api/user/cards
- 权限: user
- 响应:
```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": 1,
      "card_number": "CARD20250001",
      "card_type": "semester",
      "status": "active",
      "start_date": "2025-09-01",
      "end_date": "2026-01-31"
    }
  ]
}
```

### 2.2 管理员发放/绑定卡
- Method: POST
- URL: /api/admin/cards
- 权限: admin+
-- 请求:
```json
{
  "user_id": 123,
  "card_type": "semester",   // semester / year / lifetime
  "face_image_url": "https://your-cloud-url.com/face.jpg", // 人脸图片URL，管理员上传到微信云存储后传此字段
  "start_date": "2025-09-01",
  "card_number": "可选"
}
```
- 响应: 卡片详情
- 备注: card_number 唯一；user_id 可为空表示未绑定

### 2.3 管理员修改卡（激活/失效/延长）
- Method: PUT
- URL: /api/admin/cards/:id
- 权限: admin+
- 请求示例:
```json
{ "status": "inactive", "end_date": "2026-12-31" }
```
- 响应: 更新后的卡片

---

## 3. 课程 (Courses)

### 3.1 获取周课程列表
- Method: GET
- URL: /api/courses/week?week_start=2025-11-24&dance_type=HipHop&teacher=A
- 权限: 公开 (登录后可携带预约状态)
- 响应:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "week_start": "2025-11-24",
    "courses": [
      {
        "id": 101,
        "course_date": "2025-11-25",
        "start_time": "19:00",
        "end_time": "20:30",
        "teacher": "导师A",
        "dance_type": "HipHop",
        "capacity": 20,
        "current_participants": 12,
        "status": "published",
        "reserved": true
      }
    ]
  }
}
```

### 3.2 管理员按周批量发布
- Method: POST
- URL: /api/admin/courses/batch
- 权限: admin+
- 请求:
```json
{
  "week_start": "2025-11-24",
  "courses": [
    {"course_date":"2025-11-24","start_time":"18:00","end_time":"19:00","teacher":"A","dance_type":"Locking","capacity":20},
    {"course_date":"2025-11-24","start_time":"19:00","end_time":"20:00","teacher":"B","dance_type":"Popping","capacity":15}
  ],
  "copy_from_week_start": null
}
```
- 响应: 本周课程列表或批次标识

### 3.3 管理员编辑课程
- Method: PUT
- URL: /api/courses/:id
- 权限: admin+
- 请求示例:
```json
{ "capacity": 25, "status": "cancelled" }
```
- 响应: 更新后的课程

---

## 4. 预约 (Reservations)

### 4.1 用户预约课程
- Method: POST
- URL: /api/reservations
- 权限: user
- 请求:
```json
{ "course_id": 101 }
```
- 成功响应:
```json
{ "code": 200, "message": "success", "data": { "reservation_id": 1001, "status": "reserved" } }
```
- 失败:
  - 403 无有效卡
  - 409 已满或时间冲突
- 事务逻辑:
  1) 锁课程行 SELECT … FOR UPDATE  
  2) 校验卡有效  
  3) 校验时间冲突  
  4) 插入 reservation, 更新计数  
  5) 提交或回滚

### 4.2 取消预约
- Method: DELETE
- URL: /api/reservations/:id
- 权限: user(本人)/admin+
- 业务: status -> cancelled, 并回退课程计数
- 响应: 成功或失败

### 4.3 获取当前用户预约列表
- Method: GET
- URL: /api/reservations/my?page=1&page_size=20
- 权限: user
- 响应包含课程信息、checkin_status、创建时间、meta

### 4.4 管理员查看某课程预约名单
- Method: GET
- URL: /api/reservations/course/:id
- 权限: admin+
- 响应: 用户列表 + 签到状态

---

## 5. 签到 (Checkin)

### 5.1 管理员签到
- Method: POST
- URL: /api/checkin
- 权限: admin+
- 请求方式1（通过 reservation_id）:
```json
{
  "reservation_id": 1001,
  "method": "scan"
}
```
- 请求方式2（通过 course_id + user_id）:
```json
{
  "course_id": 101,
  "user_id": 123,
  "method": "manual"
}
```
- 成功响应:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "reservation_id": 1001,
    "course_id": 101,
    "user_id": 123,
    "checkin_status": "checked",
    "method": "scan",
    "checkin_time": "2025-11-25T19:05:00Z",
    "user_info": {
      "nickname": "张三",
      "real_name": "张三",
      "avatar": "https://..."
    }
  }
}
```
- 失败响应:
  - 400: 参数错误
  - 403: 预约状态异常
  - 404: 预约记录不存在
  - 409: 已签到

### 5.2 管理员批量签到
- Method: POST
- URL: /api/checkin/batch
- 权限: admin+
- 请求:
```json
{
  "reservation_ids": [1001, 1002, 1003]
}
```
- 成功响应:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "success_count": 2,
    "failure_count": 1,
    "success_list": [
      { "reservation_id": 1001, "user_id": 123 },
      { "reservation_id": 1002, "user_id": 124 }
    ],
    "failure_list": [
      { "reservation_id": 1003, "reason": "已签到" }
    ]
  }
}
```

---

## 6. 管理员登录与管理

### 6.1 管理员登录
- Method: POST
- URL: /api/admin/login
- 权限: 匿名
- 请求:
```json
{
  "username": "admin",
  "password": "admin123"
}
```
- 成功响应:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "token": "a1b2c3d4e5f6...",
    "role": "admin",
    "username": "admin"
  }
}
```
- 失败响应:
  - 401: 用户名或密码错误
  - 403: 账号已被禁用
- 备注：
  - 默认管理员账号：username: admin, password: admin123
  - 所有管理员拥有相同的完整权限
  - token有效期默认30天

### 6.2 管理员分页检索学员
- Method: GET
- URL: /api/admin/users?page=1&page_size=20&q=关键词&has_valid_card=true
- 权限: admin+
- 查询参数:
  - page: 页码，默认1
  - page_size: 每页数量，默认20，最大100
  - q: 搜索关键词（支持昵称、真实姓名、手机号）
  - has_valid_card: 是否有有效卡（true/false）
  - card_status: 卡片状态（active/inactive）
- 成功响应:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "list": [
      {
        "id": 123,
        "openid": "xxx",
        "nickname": "张三",
        "real_name": "张三",
        "phone": "13800138000",
        "avatar": "https://...",
        "status": "active",
        "has_valid_card": true,
        "cards": [
          {
            "id": 1,
            "card_number": "CARD2025001",
            "card_type": "semester",
            "status": "active",
            "start_date": "2025-09-01",
            "end_date": "2026-03-01",
            "is_valid": true
          }
        ]
      }
    ],
    "meta": {
      "total": 50,
      "page": 1,
      "page_size": 20
    }
  }
}
```

### 6.3 管理员发放导师卡
- Method: POST
- URL: /api/admin/cards
- 权限: admin+
-- 请求:
```json
{
  "user_id": 123,
  "card_type": "semester",
  "face_image_url": "https://your-cloud-url.com/face.jpg" // 人脸图片URL，管理员上传到微信云存储后传此字段
}
```
- 卡片类型说明:
  - semester: 学期卡（6个月）
  - year: 年卡（12个月）
  - lifetime: 终身卡（永久有效）
- 成功响应:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "card_id": 1,
    "card_number": "CARD17348920011234",
    "card_type": "semester",
    "status": "active",
    "start_date": "2025-12-19T10:00:00Z",
    "end_date": "2026-06-19T10:00:00Z",
    "user": {
      "id": 123,
      "nickname": "张三",
      "real_name": "张三"
    }
  }
}
```
- 失败响应:
  - 400: 参数错误或卡片类型无效
  - 404: 用户不存在

### 6.4 管理员批量发卡
- Method: POST
- URL: /api/admin/cards/batch
- 权限: admin+
- 请求:
```json
{
  "cards": [
    { "user_id": 123, "card_type": "semester" },
    { "user_id": 124, "card_type": "year" },
    { "user_id": 125, "card_type": "lifetime" }
  ]
}
```
- 成功响应:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "success_count": 2,
    "failure_count": 1,
    "success_list": [
      { "user_id": 123, "card_id": 1, "card_number": "CARD17348920011234" },
      { "user_id": 124, "card_id": 2, "card_number": "CARD17348920011235" }
    ],
    "failure_list": [
      { "user_id": 125, "reason": "用户不存在" }
    ]
  }
}
```

---

## 7. 统计与工具

### 7.1 周统计
- Method: GET
- URL: /api/admin/statistics/week?week_start=2025-11-24
- 权限: admin+
- 响应: 每日课程数、预约数、满员率

### 7.2 复制课程
- Method: POST
- URL: /api/admin/courses/copy?from_week=2025-11-17&to_week=2025-11-24
- 权限: admin+
- 响应: 新周课程列表

---

## 8. 认证与校验规则

### 8.1 认证机制
- Header格式: `Authorization: Bearer <token>` 或 `Authorization: <token>`
- 用户token: 微信登录后获取，用于用户端接口
- 管理员token: 管理员登录后获取，用于管理端接口
- token存储: 数据库持久化，支持有效期管理
- 登录失效: token不存在、已过期或用户/管理员状态异常时返回401

### 8.2 管理员权限
所有管理员拥有完整的系统管理权限：
- ✅ 查看学员列表
- ✅ 查看预约名单
- ✅ 课程签到（单个和批量）
- ✅ 发放导师卡（单个和批量）
- ✅ 课程管理
- ✅ 系统管理

### 8.3 业务校验规则
- 有效卡: status='active' 且 (card_type='lifetime' 或 start_date<=今天<=end_date)
- 时间冲突: 同日期且 NOT (existing.end_time <= new.start_time OR existing.start_time >= new.end_time)
- 课程满员: current_participants < capacity
- 取消限制: 开始前 N 分钟可取消(可配置)

### 8.4 错误码说明
- 401: 未认证或认证失效
- 403: 权限不足
- 409: 业务冲突（如重复预约、名额已满等）

---

## 9. 数据模型示例

User:
```json
{ "id":1, "openid":"xxx", "nickname":"昵称", "real_name":null, "phone":null, "avatar":"url", "status":"active" }
```
Card:
```json
{
  "id": 1,
  "user_id": 1,
  "card_number": "CARD2025",
  "card_type": "semester",
  "status": "active",
  "start_date": "2025-09-01",
  "end_date": "2026-01-31",
  "face_image_url": "https://your-cloud-url.com/face.jpg"
}
```
Course:
```json
{ "id":101, "course_date":"2025-11-25", "start_time":"19:00", "end_time":"20:30", "teacher":"导师A", "dance_type":"HipHop", "capacity":20, "current_participants":12, "status":"published" }
```
Reservation:
```json
{ "id":1001, "user_id":1, "course_id":101, "status":"reserved", "checkin_status":"not_checked", "created_at":"2025-11-20T10:00:00Z" }
```

---
备注:
- 所有时间建议统一存储为 UTC，前端展示本地化。
- 高并发预约可选 Redis 预扣并最终以 DB 校验为准。

